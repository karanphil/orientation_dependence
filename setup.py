from setuptools import setup

setup(
   name='mt_diffusion',
   version='1.0',
   description='A mt diffusion module',
   author='Philippe Karan',
   author_email='philippe.karan@usherbrooke.ca',
   packages=['modules'],  #same as name
   #install_requires=['greek'], #external packages as dependencies
   #scripts=[
            #'scripts'
           #]
)
